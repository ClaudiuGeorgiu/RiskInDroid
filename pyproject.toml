[project]
name = "RiskInDroid"
requires-python = ">=3.9"

[tool.ruff]
line-length = 88

[tool.ruff.lint]
extend-select = ["I"]

[[tool.mypy.overrides]]
module = [
    "joblib",
    "sklearn.*"
]
ignore_missing_imports = true
